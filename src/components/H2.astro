---
interface Props {
  styles?: string;
  color?: string;
}

const { styles = "", color = "black" } = Astro.props;
---

<h2
  class={`text-4xl font-bold tracking-tighter sm:text-5xl text-center mb-12  ${styles}`}
  style={`color: ${color}`}
>
  <slot />
</h2>
